'use client'

import {InputField} from "indicator-ui";

export default function Home() {
    return (
        <main>
            <InputField type={'test'}
                        mask={'HH:mm'}/>
        </main>
    )
}
