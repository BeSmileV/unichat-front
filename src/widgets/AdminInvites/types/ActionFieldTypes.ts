import React from "react";

export type ActionFieldPropsType = {
    icon?: React.ReactNode,
    title?: React.ReactNode,
    subtitle?: React.ReactNode,
    onClick?: React.MouseEventHandler<HTMLButtonElement>,
}